<%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, "Topic Title" %><br>
    <%= f.text_area :title, size: "50x1" %>
  </div>
<!--   <div class="field">
    <%= f.label :description, "Additional information (optional)" %><br>
    <%= f.text_area :description, size: "50x2" %>
  </div> -->
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
