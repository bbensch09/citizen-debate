<div class="well">
  <%= form_for (@closing_statement) do |f| %>
    <% if @closing_statement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@closing_statement.errors.count, "error") %> prohibited this closing_statement from being saved:</h2>

      <ul>
      <% @closing_statement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
            <%= f.label :content, "Post your closing statement." %><br>
            <%= f.cktext_area :content, rows: 4, class: 'ckeditor-content', required: true, :ckeditor => {:language => 'us'} %>
          </div>
          <div class="hidden">
            <%= f.number_field :round_id, value: @closing_statement.round_id %>
            <%= f.number_field :debate_id, value: @closing_statement.debate_id %>
            <%= f.number_field :author_id, value: @closing_statement.author_id %>
          </div>
    <%= f.submit 'Update statement', class: 'btn btn-primary btn-lg' %>
    </div>
  <% end %>
</div>
