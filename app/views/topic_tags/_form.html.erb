<%= form_for(@topic_tag) do |f| %>
  <% if @topic_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic_tag.errors.count, "error") %> prohibited this topic_tag from being saved:</h2>

      <ul>
      <% @topic_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :debate_id %><br>
    <%= f.number_field :debate_id %>
  </div>
  <div class="field">
    <%= f.label :topic_id %><br>
    <%= f.number_field :topic_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
