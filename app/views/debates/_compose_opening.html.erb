<div class="well">
  <%= form_for (@opening_statement) do |f| %>
    <div class="form-group">
            <%= f.label :content, "Post your opening statement." %><br>
            <%= f.cktext_area :content, rows: 8, class: 'ckeditor-content', required: true, :ckeditor => {:language => 'us'} %>
          </div>
          <div class="hidden">
            <%= f.number_field :round_id, value: 1 %>
            <%= f.number_field :debate_id, value: @debate.id %>
            <%= f.number_field :author_id, value: current_user.id %>
          </div>
    <%= f.submit 'Submit message', class: 'btn btn-primary btn-lg' %>
    </div>
  <% end %>
</div>
