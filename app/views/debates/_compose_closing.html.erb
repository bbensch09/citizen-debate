<!-- <div class="example-argument col-lg-9 col-lg-offset-3 col-xs-12">
  <h4>Recommended format for opening statements (brevity >>> rhetoric):</h4>
    <ol>
      <li> <b> One sentence summary of your primary argument</b>
        <ul>
          <li>source: <a href="#">Article Title (link to primary source)</a></li>
          <li>1-2 sentences of commentary expanding upon the argument (source credibility, quantitative impact, etc.)</li>
        </ul>
      </li>
      <li> <b> One sentence summary of your secondary argument</b>
        <ul>
          <li>source: <a href="#">Article Title (link to source)</a></li>
          <li>1-2 sentences of commentary expanding upon the argument</li>
        </ul>
      </li>
    </ol>
</div>
 -->
<div class="well">
  <%= form_for (@closing_statement) do |f| %>
    <div class="form-group">
            <%= f.label :content, "Post your closing statement." %><br>
            <%= f.cktext_area :content, rows: 12, class: 'ckeditor-content', required: true, :ckeditor => {:language => 'us'} %>
          </div>
          <div class="hidden">
            <%= f.number_field :round_id, value: 3 %>
            <%= f.number_field :debate_id, value: @debate.id %>
            <%= f.number_field :author_id, value: current_user.id %>
          </div>
    <%= f.submit 'Submit message', class: 'btn btn-primary btn-lg' %>
    </div>
  <% end %>
</div>
